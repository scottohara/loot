{
	globals: {
		inject: false,
		sinon: false
	},
	rules: {
		// Best practices
		no-unused-expressions: 0,																									// Disable to allow Chai should syntax

		// Stylistic issues
		max-nested-callbacks: [2, 7]
	}
}
