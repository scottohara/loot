export default class UibModalInstanceMockProvider {
	constructor() {
		// Mock $uibModalInstance object
		this.$uibModalInstance = {
			close: sinon.stub(),
			dismiss: sinon.stub()
		};
	}

	$get() {
		return this.$uibModalInstance;
	}
}

UibModalInstanceMockProvider.$inject = [];