(function() {
	"use strict";

	/**
	 * Registration
	 */
	angular
		.module("ogAngularMocks")
		.provider("$anchorScrollMock", Provider);

	/**
	 * Implementation
	 */
	function Provider() {
		var provider = this;

		// Mock $anchorScroll object
		provider.$anchorScroll = sinon.stub();

		provider.$get = function() {
			return provider.$anchorScroll;
		};
	}
})();
