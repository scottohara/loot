{
	"extends": "../tsconfig.json",
	"compilerOptions": {
		// Type Checking

		// Modules
		"baseUrl": "../",
		"paths": {
			"~*": [
				"cypress/*"
			]
		},
		"types": [
			"cypress",
			"node"
		],

		// Emit
		"sourceMap": false,

		// JavaScript Support

		// Editor Support

		// Interop Constraints

		// Backwards Compatibility

		// Language and Environment
		"target": "es2019"

		// Compiler Diagnostics

		// Projects

		// Output Formatting

		// Completeness

		// Command Line

		// Watch Options
	},
	"exclude": [
		"node_modules"
	]
}